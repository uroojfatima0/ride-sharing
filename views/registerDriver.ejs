<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register as Driver</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f9f9f9;
      /* Light, soft background color */
      padding: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
      color: #333;
    }

    h1 {
      font-size: 3em;
      color: #2b2b2b;
      margin-bottom: 40px;
      font-weight: 700;
      letter-spacing: 2px;
      text-shadow: 1px 1px 8px rgba(0, 0, 0, 0.1);
    }

    form {
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 450px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-size: 1.2em;
      color: #333;
      text-align: left;
    }

    input {
      width: 90%;
      padding: 12px;
      margin-bottom: 20px;
      font-size: 1.1em;
      border: 2px solid #ddd;
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    input:focus {
      border-color: #ff61a6;
      /* Soft pink on focus */
      box-shadow: 0 0 5px rgba(255, 97, 166, 0.6);
    }

    .input-group {
      display: flex;
      justify-content: space-between;
      gap: 20px;
      /* Optional: Adds space between the two fields */
    }

    .input-item {
      flex: 1;
      /* Makes the input fields take up 50% of the row each */
    }

    .input-item input {
      width: 90%;
      /* Makes the input field take up the full width of its container */
      padding: 12px;
      margin-bottom: 20px;
      font-size: 1.1em;
      border: 2px solid #ddd;
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    .input-item input:focus {
      border-color: #ff61a6;
      /* Soft pink on focus */
      box-shadow: 0 0 5px rgba(255, 97, 166, 0.6);
    }


    h3 {
      font-size: 1.6em;
      color: #333;
      margin-top: 30px;
      margin-bottom: 20px;
      text-align: left;
    }

    button {
      font-size: 1.2em;
      padding: 12px 40px;
      color: #fff;
      background: #ff61a6;
      /* Soft pink for the button */
      border: 2px solid transparent;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      text-transform: uppercase;
    }

    button:hover {
      background: linear-gradient(145deg, #ff61a6, #e65694);
      /* Gradient effect on hover */
      transform: translateY(-5px) scale(1.05);
    }

    button:active {
      transform: translateY(-2px) scale(1);
    }
  </style>
</head>

<body>
  <h1>Driver Registration</h1>
  <form action="/drivers/register" method="POST">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" required><br>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required><br>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required><br>

    <label for="phone">Phone Number:</label>
    <input type="text" id="phone" name="phone" required><br>

    <h3>Vehicle Details:</h3>
    <div class="input-group">
      <div class="input-item">
        <label for="licensePlate">License Plate:</label>
        <input type="text" id="licensePlate" name="vehicleDetails[licensePlate]" required>
      </div>

      <div class="input-item">
        <label for="model">Model:</label>
        <input type="text" id="model" name="vehicleDetails[model]" required>
      </div>
    </div>


    <label for="color">Color:</label>
    <input type="text" id="color" name="vehicleDetails[color]" required><br>

    <button type="submit">Register</button>
  </form>
</body>

</html>